# office-token-booster · 使用示例与配套答案

> 本文为「office-token-booster」在天禧 AI / SkillHub 上架所需的**使用示例**及其**配套答案演示**。
> 每条示例包含：用户怎么说 → 技能实际执行什么 → 输出示意 → 一句话结论。
>
> ✅ **文中所有输出均为 v0.9.11 实跑截取**（`scripts/executor.py` / `scripts/report_engine.py` 真实运行结果），非虚构示意。
> 📌 配套账本示例数据（7 条、跨越两周）用于演示，可复现。

---

## 表单填写值（4 条使用示例，每条约 20 字）

| # | 填入「使用示例」栏位的文案 | 字数 | 展示能力 |
|---|---|---|---|
| 一 | `帮我写这周的周报，重点突出完成事项和下周计划` | 22 | 执行·周报生成 |
| 二 | `整理这份会议记录，列出待办事项和负责人` | 19 | 执行·会议纪要 |
| 三 | `刚才做的任务省了多少 token 和时间？帮我记一笔` | 26 | 记账闭环（核心差异化） |
| 四 | `生成提效报告，对比这周和上周省了多少` | 18 | 提效报告 + 周期对比 |

> 备用替换（想覆盖其他能力时）：`分析这个 CSV 的销售数据，顺便导出成 Excel` / `帮我整理这份长文档的要点` / `做一份 PPT 大纲`

---

## 示例一 · 帮我写这周的周报

**用户说**

> 帮我写这周的周报，重点突出完成事项和下周计划

**技能执行**

```bash
python scripts/executor.py --type 周报生成 \
  --input 本周事件.txt --output 周报.md
```

`--input` 每行一条要点，可带「风险：」「下周计划：」等前缀，技能自动归类到对应章节。

**输出示意**（实跑截取）

```markdown
# 周报（2026-09-04）

## 重点工作

- 完成了客户续约方案的初稿，和法务对齐了条款
- 推进了数据看板二期，完成接口联调
- 参与三个需求评审，输出两版方案

## 风险与阻塞

- 第三方接口不稳定，联调时有超时
- 续约方案还差财务确认，可能延后

## 下周计划

- 完成财务确认并提交终稿
- 看板二期提测
- 整理季度复盘材料

> 由 office-token-booster 执行引擎生成；节省值请在确认写回账本时补充「笨办法」基准。
```

💡 **回答你的问题**：周报已生成，完成事项按「重点工作」分三条列出，下周计划独立成节。原始输入里带「风险：」「下周计划：」前缀的行被自动归类到对应章节，不需要你手工整理格式。整个过程**不联网、不调 LLM**，纯本地模板渲染。

---

## 示例二 · 整理这份会议记录

**用户说**

> 整理这份会议记录，列出待办事项和负责人

**技能执行**

```bash
python scripts/executor.py --type 会议纪要 \
  --input 会议记录.txt --output 纪要.md
```

**输出示意**（实跑截取）

```markdown
# 会议纪要（2026-09-04）

## 参会与背景

- 参会：产品-张伟、研发-李娜、测试-王强、运营-陈静

## 核心结论

- 结论：Q4 只做两个大版本，第三个延期到明年 Q1；次要需求本期砍掉

## 待办事项

- [ ] 待办：张伟负责输出砍需求清单，下周三前
- [ ] 待办：李娜负责评估两版本的后端排期，本周五前
- [ ] 待办：王强负责给出测试周期底线，本周五前

## 遗留问题

- （无）

> 由 office-token-booster 执行引擎生成。
```

💡 **回答你的问题**：纪要已结构化。从原始发言里抽出**核心结论 1 条**与**待办 3 条**，每条待办自带**负责人 + 截止时间**（如「张伟…下周三前」），并生成可勾选的 `- [ ]` 清单，可直接贴进项目管理工具。

---

## 示例三 · 刚做的任务省了多少？记一笔

**用户说**

> 刚才做的任务省了多少 token 和时间？帮我记一笔

**技能执行**

先**预览**（dry-run，默认行为，**不写盘**）：

```bash
python scripts/executor.py --type 会议纪要 \
  --input 会议记录.txt --output 纪要.md \
  --apply-ledger ledger.json \
  --skill-tokens 1400 --skill-minutes 7 \
  --baseline-tokens 9000 --baseline-minutes 30
```

```
[完成] 已生成 会议纪要 交付物 → 纪要.md（247 字符）
[记账] 预览（dry-run）：确认后加 --confirm-ledger 写回。
```

确认无误后再**真正写回**（加 `--confirm-ledger`）：

```bash
python scripts/executor.py --type 会议纪要 \
  --input 会议记录.txt --output 纪要.md \
  --apply-ledger ledger.json \
  --skill-tokens 1400 --skill-minutes 7 \
  --baseline-tokens 9000 --baseline-minutes 30 \
  --confirm-ledger
```

```
[记账] 已写回账本：ledger.json
```

写入的账本条目（实际 schema）：

```json
{
  "date": "2026-09-04",
  "type": "会议纪要",
  "baseline_tokens": 9000,
  "skill_tokens": 1400,
  "baseline_minutes": 30,
  "skill_minutes": 7,
  "note": "需求评审会"
}
```

💡 **回答你的问题**：这次会议纪要**省了 7,600 Token（省 84.4%）和 23 分钟（省 76.7%）**。`skill_tokens` 是用本技能的实际消耗，`baseline_tokens` 是不用技能、自己手搓的估计成本——两者之差就是「省了多少」。**默认只预览不落盘**，必须显式加 `--confirm-ledger` 才写入；缺 baseline 时护栏会直接拦截，防止污染账本。

---

## 示例四 · 生成提效报告，对比这周和上周

**用户说**

> 生成提效报告，对比这周和上周省了多少

**技能执行**

```bash
python scripts/report_engine.py ledger.json \
  --format markdown --output 提效报告.md
```

> 也支持 `--format html`（含可视化图表、明暗自适应）、`--format json`；加 `--summary` 出一句话摘要版。

**输出示意**（实跑截取，账本含 7 条任务 / 跨两周）

```markdown
# 办公室提效报告

> 生成时间：2026-09-04 21:32 ｜ 共 7 条任务记录

## 一、概览

- **节省 Token**：67.3K（基准 80.0K → 本技能 12.7K），节省 **84.1%**
- **节省时间**：156 分钟（基准 210 → 本技能 54），节省 **74.3%**

- **本期 vs 上期**（2026-W36 对比 2026-W35）：省 37.2K Token（▲ +23.6%），任务 4 次。

## 三、任务类型统计

| 类型 | 任务数 | 基准 Token | 本技能 Token | 省 Token | 省时间(分) | Token节省% | 质量分 |
|------|------|------|------|------|------|------|------|
| 数据分析 | 2 | 30.0K | 5.0K | 25.0K | 58 | 83.3% | 88 |
| 周报生成 | 2 | 24.0K | 3.6K | 20.4K | 38 | 85.0% | 88 |
| 会议纪要 | 2 | 18.0K | 2.9K | 15.1K | 45 | 83.9% | 100 |
| 文档整理 | 1 | 8.0K | 1.2K | 6.8K | 15 | 85.0% | 100 |

## 十二、质量护栏（不降质量）

> ✅ **质量护栏**：均分 93/100 ≥ 门槛 70，交付质量达标，节省可信。
```

报告还包含「Token 提效可视化」「按周节省趋势」「自动化 ROI 排序」「推荐 Skill」「下周展望」等章节。

💡 **回答你的问题**：累计省了 **67.3K Token（84.1%）和 156 分钟（74.3%）**；**本周比上周多省 23.6%**（本周 37.2K vs 上周 30.1K，本周多 1 个任务）。提效主力是**数据分析**（2 次省 25.0K，占总量 37.1%）。末尾「质量护栏」显示交付物均分 **93/100**，超过 70 分门槛——**省下来的这些是有质量保障的省**，不是偷工减料。

---

## 附：4 条示例与技能能力的对应关系

| 示例 | 覆盖的核心能力 | 对应模块 |
|---|---|---|
| 示例一 · 写周报 | 执行办公任务（模板渲染 + 要点自动归类） | `executor.render_weekly_report` |
| 示例二 · 整纪要 | 执行办公任务（结论 / 待办 / 负责人抽取） | `executor.render_meeting_minutes` |
| 示例三 · 记一笔 | 记账闭环（dry-run 预览 + 确认写回 + baseline 护栏） | `executor.propose_ledger` / `ledger_agent` |
| 示例四 · 提效报告 | 度量洞察（按类型/按周统计、周期对比、ROI、质量护栏） | `diagnose` / `report_engine` |

4 条示例覆盖「**会干活 → 会算账 → 会看趋势**」的完整闭环，正对应「执行 + 度量一体」的定位。

---

## 附：其他可用能力（示例未覆盖）

| `--type` | 用途 |
|---|---|
| `数据分析` | 分析 CSV / 表格，输出关键指标；可 `--format xlsx` 导出 Excel |
| `文档整理` | 长文提炼为要点清单 |
| `PPT大纲` | 首行主题 + 每行要点 → 生成大纲 |

**格式导出**：`--format docx`（需 `python-docx`）/ `xlsx`（需 `openpyxl`），缺失时自动降级为 `.md` / `.csv`，不报错中断。
**富文本与图表**：`--format html` 含可视化图表、明暗主题自适应。

> **安全说明**：纯标准库实现，**不联网、不读密钥、不调 LLM**；内容智能来自你提供的原始内容，数据智能来自本地计算。CI 跑四红线预检（无危险执行 / 无网络外发 / 无硬编码密钥 / 用户字段转义）。写账本默认 dry-run。
