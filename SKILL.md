---
name: office-token-booster
description: 办公室提效助手 —— 帮你把会议纪要、Excel 数据、周报、文档整理等重复办公任务一键自动化，并量化每次任务节省的 Token 与耗时。适用于办公生产力、数据分析、效率提升场景。
version: 0.1.0
author: Elisabeth15501
license: MIT
tags:
  - 办公生产力
  - 效率工具
  - 数据分析
  - token提效
categories:
  - 办公生产力
  - 数据分析
  - 效率工具
triggers:
  - 会议纪要整理
  - 会议纪要总结
  - Excel 数据分析
  - 周报生成
  - 文档提炼
  - 数据看板
  - 办公提效
  - token 提效
tools:
  - filesystem
  - shell
metadata:
  openclaw:
    requires:
      python: ">=3.10"
    env: []
    platform: ["tianxi", "openclaw", "linux", "macos", "windows"]
---

# 办公室提效助手（office-token-booster）

> 把重复办公任务交给技能，把省下的 Token 和時間看得見。

## 这个技能解决什么

办公室里大量时间花在**重复、低创意**的事情上：把一小时会议录音整理成纪要、从一张杂乱的 Excel 里算出关键指标、每周五手写周报、把长文档压成要点。

`office-token-booster` 把这些任务做成可复用的流程，**产出结构化结果的同时，量化你每次省了多少 Token、多少分钟**——让你和团队对"AI 到底帮我省了什么"有清晰的体感，而不是一句模糊的"提效了"。

## 核心卖点：Token 提效可视化

很多办公助手只说"我帮你做了"，但**不说帮了多少**。本技能内置一份「提效账本」：

- 你每次交给我们一个任务，记一笔：传统做法（自己手搓 / 反复试错）大概要花多少 Token、多少分钟；用本技能做花了多少。
- 累积后生成**提效报告**：总节省 Token、节省比例、按任务类型的节省分布、每周趋势。
- 报告纯本地生成，**不联网、不上传你的任何内容**。

> 注意：节省值是基于你填写的"基准估计"计算的参考值，不是平台用量扣费数据。它用于建立"提效体感"，不是计费凭证。

## 能做什么（MVP 范围）

| 能力 | 输入 | 产出 |
|------|------|------|
| 会议纪要整理 | 会议文字稿 / 转录文本 | 结构化纪要（议题、决议、待办、负责人、时限） |
| Excel 数据分析 | 表格文件或数据描述 | 关键指标摘要 + 简明结论 |
| 周报生成 | 本周要点列表 | 排版好的周报草稿 |
| 文档提炼 | 长文 / 多段材料 | 要点卡片 + 一页摘要 |
| 提效账本 | 任务记录 JSON | MD / HTML 提效报告（节省 Token、耗时、趋势） |

## 怎么用

直接用自然语言触发，例如：

- "帮我把这段会议录音文字整理成纪要，按议题、决议、待办列出来"
- "分析这份销售表，告诉我 top 3 地区和环比变化"
- "根据这 5 条本周进展生成周报"
- "把这 2000 字项目说明压成 10 个要点"
- "把这周的提效账本生成一份报告"

## 提效账本数据格式

把任务记录保存为 JSON（如 `ledger.json`），每一条：

```json
{
  "tasks": [
    {
      "date": "2026-08-08",
      "type": "会议纪要",
      "baseline_tokens": 12000,
      "skill_tokens": 3000,
      "baseline_minutes": 25,
      "skill_minutes": 3,
      "note": "2 小时录音转写"
    }
  ]
}
```

然后说"生成提效报告"，技能调用 `scripts/report_engine.py` 产出报告。

## 对话式提效诊断（推荐流程）

技能不仅一次性倒出报告，更支持**先给一页摘要 + 图表、再追问**的对话式诊断，让你按需深挖：

1. **一页摘要**：先输出九段报告的首页概览（节省 Token / 时间、任务类型分布、趋势），不淹没细节。
2. **可视化图表**：按任务类型的节省分布环形图 / 条形图，配合摘要一屏看完。
3. **追问**（基于诊断结果、不编造）：例如
   - "哪个任务类型节省最多？" → 按 `by_type` 排序回答
   - "按周趋势如何？" → 按 `by_week` 给出每周节省
   - "有什么建议？" → 返回诊断内核生成的 `recommendations`
   - "明细里会议纪要省了多少分钟？" → 从结构化结果精确取数

追问由 `scripts/qa.py` 处理，所有回答都**锚定在 `diagnose()` 产出的结构化 Diagnosis 上**，不做无数据支撑的猜测。

## 设计与合规

- **全部本地处理**：报告在本地生成，不向任何外部域名发送你的内容。
- **无硬编码密钥**：脚本不读取任何 API Key / Token。
- **数据最小化**：只处理你主动提供的文件，不扫描技能目录以外的系统文件。
- **开源协议**：MIT，可自由学习、修改、再分发。

## 范围说明（Non-goals）

- 不接入任何平台的用量 / 计费接口（节省值为用户自行估计的基准对比）。
- 不自动执行会改动你系统的危险操作（如删除文件、发送邮件）。
- 当前版本聚焦"办公文本类任务"，复杂 PPT 排版 / 多模态生成在后续版本扩展。
